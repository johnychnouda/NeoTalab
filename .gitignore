# OS
.DS_Store
Thumbs.db

# Env & secrets
.env
.env.*
!.env.example

# Node / Next.js
node_modules/
web/.next/
npm-debug.log*

# Laravel
backend/vendor/
backend/.env
backend/.phpunit.result.cache
backend/.phpunit.cache
backend/storage/*.key
backend/public/hot
backend/public/build

# IDE (optional — keep .vscode/ tracked if you prefer)
.idea/
*.swp

# PHPUnit / caches
*.cache

# Local deploy secrets (never commit)
deploy/secrets*.env
deploy/*.local
