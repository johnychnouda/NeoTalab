# Set on Railway → neotalab-web, then redeploy (baked in at build)
NEXT_PUBLIC_API_URL=https://YOUR-API.up.railway.app/api/v1

# Local development — copy to .env.local
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api/v1
