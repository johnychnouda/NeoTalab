export const metadata = {
  title: "Join NeoTalab — Start Your Free Trial",
  description: "Apply to join NeoTalab and automate WhatsApp orders for your business.",
};

export default function JoinLayout({ children }) {
  return children;
}
